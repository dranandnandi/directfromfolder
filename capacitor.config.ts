import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dcptaskmanagmentapp.taskmanager',
  appName: 'clinic_task_manager',
  webDir: 'dist'
};

export default config;
